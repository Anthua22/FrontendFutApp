import { TitsupDirective } from './titsup.directive';

describe('TitsupDirective', () => {
  it('should create an instance', () => {
    const directive = new TitsupDirective();
    expect(directive).toBeTruthy();
  });
});
